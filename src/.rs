DIMENSIONS = 2

split = [range.start, range.start, range.start, range.start, range.end]

/////////////////////////

dim = 0 ----------------

let pred = |body: &Body<DIMENSIONS>| body.pos.x < center.x;
let count = range.start;
split[1]

dim = 1 ----------------
